events {
    worker_connections 10240; # 调大连接数
}

http {
    keepalive_requests 10000; # 增加单个长连接可处理的请求数
    keepalive_timeout 65;

    # Other HTTP settings (e.g., logging, proxy settings, etc.)
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}
